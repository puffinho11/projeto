name: CD - Deploy no Railway

on:
  workflow_run:
    workflows: ["CI - Build & Push Docker"]
    types: [ "completed" ]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Clonar repositório
        uses: actions/checkout@v3

      - name: ⚙️ Instalar Railway CLI
        run: |
          curl -fsSL https://railway.app/install.sh | sh
          echo "$HOME/.railway/bin" >> $GITHUB_PATH

      - name: 🔑 Autenticar no Railway (novo formato)
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          export RAILWAY_TOKEN=${{ secrets.RAILWAY_TOKEN }}
          railway whoami || echo "✅ Autenticado via token com sucesso."

      - name: 🚀 Deploy automático para o Railway
        run: railway up --service projeto --environment production

      - name: 🔍 Verificar logs e status
        run: |
          echo "Aguardando aplicação subir..."
          sleep 15
          railway logs --service projeto --lines 20



